node_modules/
.superpowers/
test-results/
playwright-report/
/blob-report/
.playwright/
*.log
.DS_Store

# Rust workspace build artifacts
host/target/
# Cargo.lock is intentionally tracked — iextendd and iextend-tray are binary crates,
# so deterministic builds matter.
