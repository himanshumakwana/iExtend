<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDisplayName</key>
    <string>iExtend</string>

    <key>CFBundleIdentifier</key>
    <string>com.iextend.app</string>

    <key>CFBundleVersion</key>
    <string>1</string>

    <key>CFBundleShortVersionString</key>
    <string>0.1.0</string>

    <key>CFBundlePackageType</key>
    <string>APPL</string>

    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>

    <!-- iPad only — landscape orientations for a display extender -->
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- Hardware requirements -->
    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>arm64</string>
        <string>metal</string>
    </array>

    <!-- Require full-screen (no Split View as host) -->
    <key>UIRequiresFullScreen</key>
    <true/>

    <!-- Scenes (multi-window disabled; single full-screen window) -->
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
    </dict>

    <!-- Network access: local network for mDNS + WebRTC -->
    <key>NSBonjourServices</key>
    <array>
        <string>_iextend._tcp</string>
    </array>

    <key>NSLocalNetworkUsageDescription</key>
    <string>iExtend uses your local Wi-Fi to discover and connect to your computer.</string>

    <!-- Camera: QR code scanning for pairing -->
    <key>NSCameraUsageDescription</key>
    <string>Scan a QR code shown by your computer to pair iExtend.</string>

    <!-- App Transport Security: allow local WebRTC (DTLS-SRTP, not HTTPS) -->
    <key>NSAppTransportSecurity</key>
    <dict>
        <key>NSAllowsLocalNetworking</key>
        <true/>
    </dict>

    <!-- Metal GPU family -->
    <key>MetalCaptureEnabled</key>
    <false/>

    <!-- Background modes: audio is required to keep the Wi-Fi socket alive
         during brief backgrounding. The app suspends video decode immediately
         but keeps the control DataChannel alive for ~30 s (Spec §8.4). -->
    <key>UIBackgroundModes</key>
    <array>
        <string>audio</string>
        <string>voip</string>
    </array>

    <!-- Disable status bar in Live view (ContentView handles it) -->
    <key>UIStatusBarHidden</key>
    <false/>

    <!-- Prevent the screen from dimming during active sessions -->
    <key>UIIdleTimerDisabled</key>
    <false/>
</dict>
</plist>
